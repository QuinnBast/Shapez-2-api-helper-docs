# Decompiled game assemblies. Never commit these: they are tobspr's shipped code,
# reconstructed for reference only, and cannot be redistributed. Regenerate locally
# instead — see the README.
decompiled/

# tobspr's official sample repository, kept as a sibling checkout so it can still track
# upstream. Cloned separately, not vendored here.
shapez2-mod-samples/

# .NET build output
bin/
obj/
*.user

# Rider / Visual Studio
.idea/
.vs/
*.suo
*.userprefs

# macOS
.DS_Store
